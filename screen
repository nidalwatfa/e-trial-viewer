
// screens/HomeScreen.js
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const HomeScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>شاشة المحاكمات الإلكترونية</Text>
      <Text style={styles.subtitle}>هنا سيتم عرض قائمة المحاكمات المباشرة</Text>
      {/* هنا سيتم إضافة قائمة البطاقات لاحقًا */}
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f5f5f5', // لون خلفية فاتح
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 10,
    color: '#16213e', // لون غامق
  },
  subtitle: {
    fontSize: 16,
    color: '#666',
  },
});

export default HomeScreen;
