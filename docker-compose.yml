

```yaml
version: '3.8'

services:
  app:
    build: .
    container_name: e-trial-viewer
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
```
